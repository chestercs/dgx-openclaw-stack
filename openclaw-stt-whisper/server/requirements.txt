# ctranslate2 is installed from source in the Dockerfile (PyPI aarch64 wheel
# is CPU-only). faster-whisper declares `ctranslate2>=4.0,<5` as a dependency,
# so pip will find the existing 4.7.1 and leave it alone — no need to pin it
# here, which would tempt pip to resolve against PyPI wheels.
faster-whisper>=1.2.0
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# transformers: required by `ct2-transformers-converter` when the operator
# points STT_WHISPER_MODEL at a non-CT2 HuggingFace repo (e.g. a Hungarian
# Whisper fine-tune) and the app converts it to CT2 float16 on first boot.
# If you never swap models, this stays dormant.
transformers>=4.45
