fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2,<3
# HTTP client to talk to the user's existing ComfyUI install. httpx is async
# so the same task can submit a prompt, poll /history, and fetch /view bytes
# without blocking the FastAPI event loop.
httpx>=0.27,<1
# Pillow only for trivial PNG metadata extraction (width/height) before we
# base64-encode the image bytes back to the agent. We do not re-encode.
pillow>=10,<12
